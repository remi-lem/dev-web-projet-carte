name: 🚀 Deploy website on push

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ftp-remi-lem.alwaysdata.net
        server-dir: /www/
        username: remi-lem
        password: ${{ secrets.ALW_FTP_PASS }}
        exclude: |
          **/.git*
          **/.git*/**
          **/vendor/**
          **/var/**
          **/bin/**
          **/config/**
          **/composer.*
          **/symfony.*
          **/public/index.php
          **/src/Kernel.php
          **/src/Entity/**
          **/src/Repository/**
          
